GROQ_API_KEY=your_groq_api_key_here
GROQ_MODEL=openai/gpt-oss-120b
# Low on purpose: grounded record Q&A, not creative generation.
GROQ_TEMPERATURE=0.2
GROQ_MAX_TOKENS=1024
# Optional. Default lives in app/config.py (Indonesian post-visit persona with
# six safety rules). Override only to tune wording, not to remove rules.
# SYSTEM_PROMPT=...
DATABASE_URL=sqlite:///rekam_medis.db
